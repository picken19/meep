## Connect to the Repository ##

ssh -T git@github.com
enter password

## Get a Certain Branch of Code ##


git fetch origin [remote-branch]:[new-local-branch] #pull a new branch from a remote repository#

## Add, Commit, Push files to the Repository ##
git add <file> #any new files#
git commit -m "message"
git push origin #changes meep in repository (online update)#

## Merge ##
git merge xyz #merges 

Example

Caitlyns-MacBook-Pro:meep caitlynpickens$ git fetch git://github.com/SheikGeek/meep.git mast\
er:SheikGeek_master
From git://github.com/SheikGeek/meep
 * [new branch]      master     -> SheikGeek_master
Caitlyns-MacBook-Pro:meep caitlynpickens$ git branch
  SheikGeek_master
  angela
  hkb261
  ic8
* master
  picken19

Caitlyns-MacBook-Pro:meep caitlynpickens$ git checkout SheikGeek_master
error: Your local changes to the following files would be overwritten by checkout:
       meep_example_app.py
       pickle/message_0.pickle
       pickle/message_1.pickle
Please, commit your changes or stash them before you can switch branches.
error: The following untracked working tree files would be overwritten by checkout:
       meepcookie.py
       util/TinyHTTPProxy/LICENSE.txt
       util/TinyHTTPProxy/TinyHTTPProxy.py
Please move or remove them before you can switch branches.
Aborting

Caitlyns-MacBook-Pro:meep caitlynpickens$ git checkout SheikGeek_master
Switched to branch 'SheikGeek_master'


## Status Outputs ##
git log #shows the history of the repository#
git status #shows the difference in status between my code committed and the master#
git diff #shows the differences between my (local?) code and the master#
git branch #shows a list of optional branches#


